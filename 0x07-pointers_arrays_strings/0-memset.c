#include "main.h"

/**
 * _memset - Entry point
 * @s: pointed destination
 * @b: constant byte
 * @n: bytes
 * Return: always 0 (success)
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;
	char *dest;
	int n;
	int b;
	char s;
	
	for (i = 0; i < n; i++)
		s[i] = b;
	return (s);
}
